export class Photo {
    albumId : number
    id : number
    title : string
    url : string
    thumbnailUrl : string

    // constructor(data) {
    //     this.albumId = data && data.albumId ? data.albumId : null;
    //     this.id = data && data.id ? data.id : null;
    //     this.title = data && data.title ? data.title : null;
    //     this.url = data && data.url ? data.url : null;
    //     this.thumbnailUrl = data && data.thumbnailUrl ? data.thumbnailUrl : null;
    // }
}